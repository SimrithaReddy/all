
function smallnumbers(arr,n){
    let map = new Map();
    for(let i=0; i<n; i++){
        map.set(arr[i],0)
    }
    for(let ele of map.keys()){
        for(let ele of map.keys()){
            if(ele>next().ele)
        }
    }
}

let n = 5;
let arr = [8,1,2,2,3]
let res = smallnumbers(arr,n)
console.log(smallnumbers(arr,n))